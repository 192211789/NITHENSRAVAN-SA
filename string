#include<stdio.h>
int main()
{
	char sl[100], s2[100], i;
	printf("enter string sl: ");
	fgets(sl, sizeof(sl), stdin);
	
	for(i= 0; sl[i] !='\0'; ++i)
	{
		s2[i] = s1[i];
	}
	s2[i] = '\0';
	printf("srtring s2: %s",s2);
	return 0;
}
