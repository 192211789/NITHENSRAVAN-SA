#include<stdio.h>
#include<conio.h>
int main()
{
	int arr[10],small,i;
	printf("enter 10 array elements:");
	for (i=0;i<10;i++)
	scanf("%d",&arr[i]);
	i=0;
	small =arr[i];
	while(i<10)
	{
		if(small>arr[i])
		small=arr[i];
		i++;
	}
	printf("\nsmallest number=%d",small);
	getch();
	return 0;
}
